# Install Supabase Agent Skills

Do not run this from the scaffold template automatically.

Run this only inside a project that actually uses Supabase:

```sh
npx skills add supabase/agent-skills
```

Use Supabase's official skills package as the source of truth. Refresh vendor skills from the official source rather than manually copying or editing them.
